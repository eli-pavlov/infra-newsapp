name: 'Bootstrap: State Bucket'
on:
  workflow_dispatch: {}

jobs:
  apply:
    name: Create OCI State Bucket
    uses: ./.github/workflows/reusable-terraform.yml
    with:
      workspace-path: "terraform/0-bootstrap"
      action: "apply"
    secrets: inherit # Pass all secrets from this workflow to the reusable one