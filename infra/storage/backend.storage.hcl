bucket    = "YOUR_BACKEND_BUCKET"    # workflows pass real values; keep for local testing
namespace = "YOUR_NAMESPACE"
region    = "YOUR_REGION"
key       = "storage.tfstate"   # explicit dedicated key
